# Must be included before using e.g. PORT_EXAMPLE
include ../ports.mk

# Files that should be included in handout.tar.gz
HANDOUT=INTRO.md knaek_mig

# Docker image name
DOCKER_NAME=knaek_mig

clean:
	rm -f handout.tar.xz knaek_mig

# Must be included at the bottom
include ../build.mk
